<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidstudyrecord">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidStudyRecord">
        <activity
            android:name=".materialdesign.MaterialDesign"
            android:theme="@style/AppTheme"
            android:label="Fruits">
        </activity>
        <activity
            android:name=".materialdesign.FruitActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".ui.UiActivity" />
        <activity android:name=".criminalintent.CriminalIntentActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>